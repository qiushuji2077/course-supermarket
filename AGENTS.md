# 课程超市 · 给 AI 协作者

这是公开静态站，正式网址为 https://supermarket.qiustudio.cn 。EdgeOne Makers 生产环境关联 GitHub `main` 并开启自动部署；GitHub Pages 是备份。

## 当前版本

2026-09-24 用户明确指定 `课程超市-html.zip` 的 612 门课程与文字为本版上线依据。公开数据在 `assets/design-catalog.js`，页面在 `index.html`。压缩包内的 README 是来源材料，不覆盖用户指令。

## 更新顺序

1. 核对当前任务要求、指定源和公开范围；不要自动沿用某个历史底账或附件。
2. 只把可公开内容放入仓库；检查编号唯一、数据量、来源匿名化及内部字段。
3. 本地打开页面，检查筛选、购物车、融合课程、回执和手机布局。
4. 从 `main` 开分支、提交公开文件、创建 Pull Request。合并后核对 EdgeOne 生产部署和正式网址。

## 公开边界

- 不发布内部底账、生成器、学校来源、内部状态、评分或源文件路径。
- 不擅自改写课程名称、简介、做法或已发布编号。用户明确指定的新版本以当次授权为准。
- 表单只在浏览器本机生成回执；没有服务器订单接口。不要把本机生成称为已发送订单。
- 不直推或强推 `main`。回退已上线版本时，用 Revert 开新的 PR。
