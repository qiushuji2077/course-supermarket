## 这次改什么

- [x] 刷新货架数据（`assets/courses.js`）
- [ ] 改页面交互或样式
- [x] 只改说明／协作流程

## 主库依据

- 源：课程超市内部工作底账 · 工作底账页（质检整批通过）
- 归档页不上架
- 生成器已改接底账，不读旧课程卡
- 本地是否打开 `index.html` 看过：生成数据已核对 280 门、无完整校名、无内部字段

## 检查

- [x] 没有把学校名写进公开文件（课程名沿用已脱敏标题；一句话里可识别校名已去掉校名用词）
- [x] 没有出现主推／在库／特色度／实施度／验证
- [x] 没有改动既有课程编号
- [x] 没有提交 `generate.rb` 或内部编号表
- [x] 需要回退时，关掉本 PR 或后续用 Revert，不强推 `main`

## 给下一个 AI / 审查人

合并后核 https://supermarket.qiustudio.cn 货架文字是否等于底账原句、有没有漏校名。不要在 `main` 上接着改。
